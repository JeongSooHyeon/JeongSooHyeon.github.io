---
title:  "TIL - 2026/01/12/월"

categories:
  - Til
tags:
  - [Programming]

toc: true
toc_sticky: false
 
date: 2026-01-12
last_modified_at: 2026-01-12
---
<br>

# 📚 TIL (Today I Learned)

## 1. 객체 지향 및 설계 원칙

### 필드에서 구현체 대신 인터페이스에 의존하기
- **내용**: 클래스 내부 필드나 변수를 선언할 때 구체적인 구현 클래스(예: `ArrayList`) 대신 인터페이스(예: `List`)를 타입으로 사용한다.
- **장점**: 코드의 유연성이 높아지며, 나중에 구현체가 변경되어도 이를 사용하는 코드를 수정할 필요가 없어 결합도가 낮아진다. (DIP: 의존 역전 원칙 준수)

### 파라미터 최적화 (데이터 최소화)
- **내용**: 너무 큰 자료구조(예: `Map<UUID, Message>`)를 통째로 넘기기보다는, 필요한 객체(`Message`)만 골라서 전달한다.
- **장점**: 메모리 낭비를 줄이고, 메서드의 목적을 명확히 하며 재사용성을 높인다.

### 공통 필드 추상화
- **내용**: 여러 클래스에서 공통적으로 사용되는 필드(예: `createdAt`, `updatedAt`, `id`)는 하나의 상위 클래스로 묶어 관리한다.
- **장점**: 코드 중복을 제거하고 유지보수를 용이하게 한다.

---

## 2. 데이터 관리 및 검증

### 조회, 수정, 삭제 시 유효성 검증
- **내용**: 모든 데이터 조작 단계에서 권한 확인 및 데이터의 유효성(형식, 존재 여부 등)을 반드시 검증해야 한다.
- **목적**: 시스템의 안정성을 확보하고 잘못된 데이터가 DB에 반영되는 것을 방지한다.

### 비즈니스 로직 (Business Logic)
- **정의**: 데이터 저장 방식(DB 종류 등)과 관계없이 **우리 서비스가 반드시 지켜야 하는 핵심 규칙**이다.
- **주요 역할**:
    - **유효성 검사**: 입력값이 비즈니스 조건에 맞는지 확인 (예: 주문 금액이 0원 이상인지 확인)
    - **데이터 가공**: 데이터를 목적에 맞게 변환하거나 계산 (예: 할인율 적용)

---

## 3. 기술 개념

### 데이터 직렬화 (Serialization)
- **정의**: 자바(Java) 시스템 내부에서 사용되는 객체를 외부의 시스템에서도 사용할 수 있도록 **바이트 스트림(Byte Stream)** 형태로 변환하는 과정이다.
- **용도**: 객체를 파일로 저장하거나, 네트워크를 통해 다른 서버로 전송할 때 주로 사용된다.

---

## 🔗 참고 자료
- [Codeit Notion 학습 자료](https://codeit.notion.site/13b6fd228e8d80c6b144cdfbf518a9f7)